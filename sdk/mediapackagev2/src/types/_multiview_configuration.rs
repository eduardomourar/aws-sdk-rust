// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The multiview configuration for a channel. A multiview channel composites video from several source channels into a single tiled output stream. Players receive one standard HLS or DASH stream instead of several separate streams. This setting is required when <code>InputType</code> is <code>MULTIVIEW</code>, and can't be set for any other input type.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct MultiviewConfiguration {
    /// <p>The channels that players can use as tiles in this multiview channel's output. Each source channel must be in the same channel group as the multiview channel, and must have an <code>InputType</code> of <code>CMAF</code>. Only the channels that you list here are available as tiles.</p>
    pub available_sources: ::std::vec::Vec<::std::string::String>,
    /// <p>The tile layouts that players can request from this multiview channel's origin endpoints. Only the layouts that you list here are available. Each layout must appear at most once.</p>
    pub available_layouts: ::std::vec::Vec<crate::types::MultiviewLayoutType>,
}
impl MultiviewConfiguration {
    /// <p>The channels that players can use as tiles in this multiview channel's output. Each source channel must be in the same channel group as the multiview channel, and must have an <code>InputType</code> of <code>CMAF</code>. Only the channels that you list here are available as tiles.</p>
    pub fn available_sources(&self) -> &[::std::string::String] {
        use std::ops::Deref;
        self.available_sources.deref()
    }
    /// <p>The tile layouts that players can request from this multiview channel's origin endpoints. Only the layouts that you list here are available. Each layout must appear at most once.</p>
    pub fn available_layouts(&self) -> &[crate::types::MultiviewLayoutType] {
        use std::ops::Deref;
        self.available_layouts.deref()
    }
}
impl MultiviewConfiguration {
    /// Creates a new builder-style object to manufacture [`MultiviewConfiguration`](crate::types::MultiviewConfiguration).
    pub fn builder() -> crate::types::builders::MultiviewConfigurationBuilder {
        crate::types::builders::MultiviewConfigurationBuilder::default()
    }
}

/// A builder for [`MultiviewConfiguration`](crate::types::MultiviewConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct MultiviewConfigurationBuilder {
    pub(crate) available_sources: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) available_layouts: ::std::option::Option<::std::vec::Vec<crate::types::MultiviewLayoutType>>,
}
impl MultiviewConfigurationBuilder {
    /// Appends an item to `available_sources`.
    ///
    /// To override the contents of this collection use [`set_available_sources`](Self::set_available_sources).
    ///
    /// <p>The channels that players can use as tiles in this multiview channel's output. Each source channel must be in the same channel group as the multiview channel, and must have an <code>InputType</code> of <code>CMAF</code>. Only the channels that you list here are available as tiles.</p>
    pub fn available_sources(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.available_sources.unwrap_or_default();
        v.push(input.into());
        self.available_sources = ::std::option::Option::Some(v);
        self
    }
    /// <p>The channels that players can use as tiles in this multiview channel's output. Each source channel must be in the same channel group as the multiview channel, and must have an <code>InputType</code> of <code>CMAF</code>. Only the channels that you list here are available as tiles.</p>
    pub fn set_available_sources(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.available_sources = input;
        self
    }
    /// <p>The channels that players can use as tiles in this multiview channel's output. Each source channel must be in the same channel group as the multiview channel, and must have an <code>InputType</code> of <code>CMAF</code>. Only the channels that you list here are available as tiles.</p>
    pub fn get_available_sources(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.available_sources
    }
    /// Appends an item to `available_layouts`.
    ///
    /// To override the contents of this collection use [`set_available_layouts`](Self::set_available_layouts).
    ///
    /// <p>The tile layouts that players can request from this multiview channel's origin endpoints. Only the layouts that you list here are available. Each layout must appear at most once.</p>
    pub fn available_layouts(mut self, input: crate::types::MultiviewLayoutType) -> Self {
        let mut v = self.available_layouts.unwrap_or_default();
        v.push(input);
        self.available_layouts = ::std::option::Option::Some(v);
        self
    }
    /// <p>The tile layouts that players can request from this multiview channel's origin endpoints. Only the layouts that you list here are available. Each layout must appear at most once.</p>
    pub fn set_available_layouts(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::MultiviewLayoutType>>) -> Self {
        self.available_layouts = input;
        self
    }
    /// <p>The tile layouts that players can request from this multiview channel's origin endpoints. Only the layouts that you list here are available. Each layout must appear at most once.</p>
    pub fn get_available_layouts(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::MultiviewLayoutType>> {
        &self.available_layouts
    }
    /// Consumes the builder and constructs a [`MultiviewConfiguration`](crate::types::MultiviewConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`available_sources`](crate::types::builders::MultiviewConfigurationBuilder::available_sources)
    /// - [`available_layouts`](crate::types::builders::MultiviewConfigurationBuilder::available_layouts)
    pub fn build(self) -> ::std::result::Result<crate::types::MultiviewConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::MultiviewConfiguration {
            available_sources: self.available_sources.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "available_sources",
                    "available_sources was not specified but it is required when building MultiviewConfiguration",
                )
            })?,
            available_layouts: self.available_layouts.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "available_layouts",
                    "available_layouts was not specified but it is required when building MultiviewConfiguration",
                )
            })?,
        })
    }
}
