// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// When writing a match expression against `TimeGranularity`, it is important to ensure
/// your code is forward-compatible. That is, if a match arm handles a case for a
/// feature that is supported by the service but has not been represented as an enum
/// variant in a current version of SDK, your code should continue to work when you
/// upgrade SDK to a future version in which the enum does include a variant for that
/// feature.
///
/// Here is an example of how you can make a match expression forward-compatible:
///
/// ```text
/// # let timegranularity = unimplemented!();
/// match timegranularity {
///     TimeGranularity::Monthly => { /* ... */ },
///     TimeGranularity::QuarterlyCalendar => { /* ... */ },
///     TimeGranularity::QuarterlyFiscal => { /* ... */ },
///     TimeGranularity::YearlyCalendar => { /* ... */ },
///     TimeGranularity::YearlyFiscal => { /* ... */ },
///     other @ _ if other.as_str() == "NewFeature" => { /* handles a case for `NewFeature` */ },
///     _ => { /* ... */ },
/// }
/// ```
/// The above code demonstrates that when `timegranularity` represents
/// `NewFeature`, the execution path will lead to the second last match arm,
/// even though the enum does not contain a variant `TimeGranularity::NewFeature`
/// in the current version of SDK. The reason is that the variable `other`,
/// created by the `@` operator, is bound to
/// `TimeGranularity::Unknown(UnknownVariantValue("NewFeature".to_owned()))`
/// and calling `as_str` on it yields `"NewFeature"`.
/// This match expression is forward-compatible when executed with a newer
/// version of SDK where the variant `TimeGranularity::NewFeature` is defined.
/// Specifically, when `timegranularity` represents `NewFeature`,
/// the execution path will hit the second last match arm as before by virtue of
/// calling `as_str` on `TimeGranularity::NewFeature` also yielding `"NewFeature"`.
///
/// Explicitly matching on the `Unknown` variant should
/// be avoided for two reasons:
/// - The inner data `UnknownVariantValue` is opaque, and no further information can be extracted.
/// - It might inadvertently shadow other intended match arms.
///
/// <p>Specifies the time period over which environmental impact data is aggregated.</p>
#[non_exhaustive]
#[derive(
    ::std::clone::Clone, ::std::cmp::Eq, ::std::cmp::Ord, ::std::cmp::PartialEq, ::std::cmp::PartialOrd, ::std::fmt::Debug, ::std::hash::Hash,
)]
pub enum TimeGranularity {
    /// <p>Environmental impact data aggregated by calendar month.</p>
    Monthly,
    /// <p>Environmental impact data aggregated over calendar quarter periods (Q1: Jan-Mar, Q2: Apr-Jun, Q3: Jul-Sep, Q4: Oct-Dec).</p>
    QuarterlyCalendar,
    /// <p>Environmental impact data aggregated over fiscal quarter periods based on the fiscal year start month specified in GranularityConfiguration.FiscalYearStartMonth.</p>
    QuarterlyFiscal,
    /// <p>Environmental impact data aggregated over calendar year periods (January-December).</p>
    YearlyCalendar,
    /// <p>Environmental impact data aggregated over fiscal year periods starting from the month specified in GranularityConfiguration.FiscalYearStartMonth.</p>
    YearlyFiscal,
    /// `Unknown` contains new variants that have been added since this code was generated.
    #[deprecated(note = "Don't directly match on `Unknown`. See the docs on this enum for the correct way to handle unknown variants.")]
    Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue),
}
impl ::std::convert::From<&str> for TimeGranularity {
    fn from(s: &str) -> Self {
        match s {
            "MONTHLY" => TimeGranularity::Monthly,
            "QUARTERLY_CALENDAR" => TimeGranularity::QuarterlyCalendar,
            "QUARTERLY_FISCAL" => TimeGranularity::QuarterlyFiscal,
            "YEARLY_CALENDAR" => TimeGranularity::YearlyCalendar,
            "YEARLY_FISCAL" => TimeGranularity::YearlyFiscal,
            other => TimeGranularity::Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue(other.to_owned())),
        }
    }
}
impl ::std::str::FromStr for TimeGranularity {
    type Err = ::std::convert::Infallible;

    fn from_str(s: &str) -> ::std::result::Result<Self, <Self as ::std::str::FromStr>::Err> {
        ::std::result::Result::Ok(TimeGranularity::from(s))
    }
}
impl TimeGranularity {
    /// Returns the `&str` value of the enum member.
    pub fn as_str(&self) -> &str {
        match self {
            TimeGranularity::Monthly => "MONTHLY",
            TimeGranularity::QuarterlyCalendar => "QUARTERLY_CALENDAR",
            TimeGranularity::QuarterlyFiscal => "QUARTERLY_FISCAL",
            TimeGranularity::YearlyCalendar => "YEARLY_CALENDAR",
            TimeGranularity::YearlyFiscal => "YEARLY_FISCAL",
            TimeGranularity::Unknown(value) => value.as_str(),
        }
    }
    /// Returns all the `&str` representations of the enum members.
    pub const fn values() -> &'static [&'static str] {
        &["MONTHLY", "QUARTERLY_CALENDAR", "QUARTERLY_FISCAL", "YEARLY_CALENDAR", "YEARLY_FISCAL"]
    }
}
impl ::std::convert::AsRef<str> for TimeGranularity {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}
impl TimeGranularity {
    /// Parses the enum value while disallowing unknown variants.
    ///
    /// Unknown variants will result in an error.
    pub fn try_parse(value: &str) -> ::std::result::Result<Self, crate::error::UnknownVariantError> {
        match Self::from(value) {
            #[allow(deprecated)]
            Self::Unknown(_) => ::std::result::Result::Err(crate::error::UnknownVariantError::new(value)),
            known => Ok(known),
        }
    }
}
impl ::std::fmt::Display for TimeGranularity {
    fn fmt(&self, f: &mut ::std::fmt::Formatter) -> ::std::fmt::Result {
        match self {
            TimeGranularity::Monthly => write!(f, "MONTHLY"),
            TimeGranularity::QuarterlyCalendar => write!(f, "QUARTERLY_CALENDAR"),
            TimeGranularity::QuarterlyFiscal => write!(f, "QUARTERLY_FISCAL"),
            TimeGranularity::YearlyCalendar => write!(f, "YEARLY_CALENDAR"),
            TimeGranularity::YearlyFiscal => write!(f, "YEARLY_FISCAL"),
            TimeGranularity::Unknown(value) => write!(f, "{value}"),
        }
    }
}
