// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The messaging limits that apply to an origination identity, such as a phone number, sender ID, or RCS agent. Includes the per-capability send rates and, for supported origination identities, advisory per-provider daily message caps.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct MessagingLimits {
    /// <p>The maximum send rate for each supported capability, in messages per second. The map is keyed by capability, such as <code>SMS</code>, <code>MMS</code>, <code>VOICE</code>, or <code>RCS</code>.</p>
    pub rate_limits: ::std::option::Option<::std::collections::HashMap<::std::string::String, i64>>,
    /// <p>The advisory maximum number of messages that can be sent per day, keyed by provider (for example, <code>T-MOBILE</code>). Applies to 10DLC phone numbers and is omitted when no daily cap applies.</p>
    pub daily_message_caps: ::std::option::Option<::std::collections::HashMap<::std::string::String, i64>>,
}
impl MessagingLimits {
    /// <p>The maximum send rate for each supported capability, in messages per second. The map is keyed by capability, such as <code>SMS</code>, <code>MMS</code>, <code>VOICE</code>, or <code>RCS</code>.</p>
    pub fn rate_limits(&self) -> ::std::option::Option<&::std::collections::HashMap<::std::string::String, i64>> {
        self.rate_limits.as_ref()
    }
    /// <p>The advisory maximum number of messages that can be sent per day, keyed by provider (for example, <code>T-MOBILE</code>). Applies to 10DLC phone numbers and is omitted when no daily cap applies.</p>
    pub fn daily_message_caps(&self) -> ::std::option::Option<&::std::collections::HashMap<::std::string::String, i64>> {
        self.daily_message_caps.as_ref()
    }
}
impl MessagingLimits {
    /// Creates a new builder-style object to manufacture [`MessagingLimits`](crate::types::MessagingLimits).
    pub fn builder() -> crate::types::builders::MessagingLimitsBuilder {
        crate::types::builders::MessagingLimitsBuilder::default()
    }
}

/// A builder for [`MessagingLimits`](crate::types::MessagingLimits).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct MessagingLimitsBuilder {
    pub(crate) rate_limits: ::std::option::Option<::std::collections::HashMap<::std::string::String, i64>>,
    pub(crate) daily_message_caps: ::std::option::Option<::std::collections::HashMap<::std::string::String, i64>>,
}
impl MessagingLimitsBuilder {
    /// Adds a key-value pair to `rate_limits`.
    ///
    /// To override the contents of this collection use [`set_rate_limits`](Self::set_rate_limits).
    ///
    /// <p>The maximum send rate for each supported capability, in messages per second. The map is keyed by capability, such as <code>SMS</code>, <code>MMS</code>, <code>VOICE</code>, or <code>RCS</code>.</p>
    pub fn rate_limits(mut self, k: impl ::std::convert::Into<::std::string::String>, v: i64) -> Self {
        let mut hash_map = self.rate_limits.unwrap_or_default();
        hash_map.insert(k.into(), v);
        self.rate_limits = ::std::option::Option::Some(hash_map);
        self
    }
    /// <p>The maximum send rate for each supported capability, in messages per second. The map is keyed by capability, such as <code>SMS</code>, <code>MMS</code>, <code>VOICE</code>, or <code>RCS</code>.</p>
    pub fn set_rate_limits(mut self, input: ::std::option::Option<::std::collections::HashMap<::std::string::String, i64>>) -> Self {
        self.rate_limits = input;
        self
    }
    /// <p>The maximum send rate for each supported capability, in messages per second. The map is keyed by capability, such as <code>SMS</code>, <code>MMS</code>, <code>VOICE</code>, or <code>RCS</code>.</p>
    pub fn get_rate_limits(&self) -> &::std::option::Option<::std::collections::HashMap<::std::string::String, i64>> {
        &self.rate_limits
    }
    /// Adds a key-value pair to `daily_message_caps`.
    ///
    /// To override the contents of this collection use [`set_daily_message_caps`](Self::set_daily_message_caps).
    ///
    /// <p>The advisory maximum number of messages that can be sent per day, keyed by provider (for example, <code>T-MOBILE</code>). Applies to 10DLC phone numbers and is omitted when no daily cap applies.</p>
    pub fn daily_message_caps(mut self, k: impl ::std::convert::Into<::std::string::String>, v: i64) -> Self {
        let mut hash_map = self.daily_message_caps.unwrap_or_default();
        hash_map.insert(k.into(), v);
        self.daily_message_caps = ::std::option::Option::Some(hash_map);
        self
    }
    /// <p>The advisory maximum number of messages that can be sent per day, keyed by provider (for example, <code>T-MOBILE</code>). Applies to 10DLC phone numbers and is omitted when no daily cap applies.</p>
    pub fn set_daily_message_caps(mut self, input: ::std::option::Option<::std::collections::HashMap<::std::string::String, i64>>) -> Self {
        self.daily_message_caps = input;
        self
    }
    /// <p>The advisory maximum number of messages that can be sent per day, keyed by provider (for example, <code>T-MOBILE</code>). Applies to 10DLC phone numbers and is omitted when no daily cap applies.</p>
    pub fn get_daily_message_caps(&self) -> &::std::option::Option<::std::collections::HashMap<::std::string::String, i64>> {
        &self.daily_message_caps
    }
    /// Consumes the builder and constructs a [`MessagingLimits`](crate::types::MessagingLimits).
    pub fn build(self) -> crate::types::MessagingLimits {
        crate::types::MessagingLimits {
            rate_limits: self.rate_limits,
            daily_message_caps: self.daily_message_caps,
        }
    }
}
