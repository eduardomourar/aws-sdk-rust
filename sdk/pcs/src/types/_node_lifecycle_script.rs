// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A script to run during a compute node lifecycle stage.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct NodeLifecycleScript {
    /// <p>A unique name for the script. The name can be up to 64 characters long. Valid characters are letters, numbers, spaces, underscores (<code>_</code>), and hyphens (<code>-</code>). The first character must be a letter or a number.</p>
    pub name: ::std::string::String,
    /// <p>The source location and integrity information for the script.</p>
    pub script_source: ::std::option::Option<crate::types::ScriptSource>,
    /// <p>The command-line arguments to pass to the script. You can specify up to 20 arguments, and each argument can be up to 256 characters long.</p>
    pub arguments: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    /// <p>The behavior when the script fails. The default value is <code>TERMINATE</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>TERMINATE</code> – Terminates the compute node.</p></li>
    /// <li>
    /// <p><code>STOP_SEQUENCE</code> – Stops running subsequent scripts in the sequence but doesn't terminate the compute node.</p></li>
    /// <li>
    /// <p><code>CONTINUE</code> – Ignores the error and continues running the next script.</p></li>
    /// </ul>
    pub on_error: crate::types::OnError,
    /// <p>The policy that determines when the script runs. The default value is <code>FIRST_BOOT_ONLY</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FIRST_BOOT_ONLY</code> – Runs the script only the first time the compute node boots.</p></li>
    /// <li>
    /// <p><code>EVERY_BOOT</code> – Runs the script every time the compute node boots, including reboots.</p></li>
    /// </ul>
    pub execution_policy: crate::types::ExecutionPolicy,
}
impl NodeLifecycleScript {
    /// <p>A unique name for the script. The name can be up to 64 characters long. Valid characters are letters, numbers, spaces, underscores (<code>_</code>), and hyphens (<code>-</code>). The first character must be a letter or a number.</p>
    pub fn name(&self) -> &str {
        use std::ops::Deref;
        self.name.deref()
    }
    /// <p>The source location and integrity information for the script.</p>
    pub fn script_source(&self) -> ::std::option::Option<&crate::types::ScriptSource> {
        self.script_source.as_ref()
    }
    /// <p>The command-line arguments to pass to the script. You can specify up to 20 arguments, and each argument can be up to 256 characters long.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.arguments.is_none()`.
    pub fn arguments(&self) -> &[::std::string::String] {
        self.arguments.as_deref().unwrap_or_default()
    }
    /// <p>The behavior when the script fails. The default value is <code>TERMINATE</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>TERMINATE</code> – Terminates the compute node.</p></li>
    /// <li>
    /// <p><code>STOP_SEQUENCE</code> – Stops running subsequent scripts in the sequence but doesn't terminate the compute node.</p></li>
    /// <li>
    /// <p><code>CONTINUE</code> – Ignores the error and continues running the next script.</p></li>
    /// </ul>
    pub fn on_error(&self) -> &crate::types::OnError {
        &self.on_error
    }
    /// <p>The policy that determines when the script runs. The default value is <code>FIRST_BOOT_ONLY</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FIRST_BOOT_ONLY</code> – Runs the script only the first time the compute node boots.</p></li>
    /// <li>
    /// <p><code>EVERY_BOOT</code> – Runs the script every time the compute node boots, including reboots.</p></li>
    /// </ul>
    pub fn execution_policy(&self) -> &crate::types::ExecutionPolicy {
        &self.execution_policy
    }
}
impl NodeLifecycleScript {
    /// Creates a new builder-style object to manufacture [`NodeLifecycleScript`](crate::types::NodeLifecycleScript).
    pub fn builder() -> crate::types::builders::NodeLifecycleScriptBuilder {
        crate::types::builders::NodeLifecycleScriptBuilder::default()
    }
}

/// A builder for [`NodeLifecycleScript`](crate::types::NodeLifecycleScript).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct NodeLifecycleScriptBuilder {
    pub(crate) name: ::std::option::Option<::std::string::String>,
    pub(crate) script_source: ::std::option::Option<crate::types::ScriptSource>,
    pub(crate) arguments: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) on_error: ::std::option::Option<crate::types::OnError>,
    pub(crate) execution_policy: ::std::option::Option<crate::types::ExecutionPolicy>,
}
impl NodeLifecycleScriptBuilder {
    /// <p>A unique name for the script. The name can be up to 64 characters long. Valid characters are letters, numbers, spaces, underscores (<code>_</code>), and hyphens (<code>-</code>). The first character must be a letter or a number.</p>
    /// This field is required.
    pub fn name(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.name = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>A unique name for the script. The name can be up to 64 characters long. Valid characters are letters, numbers, spaces, underscores (<code>_</code>), and hyphens (<code>-</code>). The first character must be a letter or a number.</p>
    pub fn set_name(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.name = input;
        self
    }
    /// <p>A unique name for the script. The name can be up to 64 characters long. Valid characters are letters, numbers, spaces, underscores (<code>_</code>), and hyphens (<code>-</code>). The first character must be a letter or a number.</p>
    pub fn get_name(&self) -> &::std::option::Option<::std::string::String> {
        &self.name
    }
    /// <p>The source location and integrity information for the script.</p>
    /// This field is required.
    pub fn script_source(mut self, input: crate::types::ScriptSource) -> Self {
        self.script_source = ::std::option::Option::Some(input);
        self
    }
    /// <p>The source location and integrity information for the script.</p>
    pub fn set_script_source(mut self, input: ::std::option::Option<crate::types::ScriptSource>) -> Self {
        self.script_source = input;
        self
    }
    /// <p>The source location and integrity information for the script.</p>
    pub fn get_script_source(&self) -> &::std::option::Option<crate::types::ScriptSource> {
        &self.script_source
    }
    /// Appends an item to `arguments`.
    ///
    /// To override the contents of this collection use [`set_arguments`](Self::set_arguments).
    ///
    /// <p>The command-line arguments to pass to the script. You can specify up to 20 arguments, and each argument can be up to 256 characters long.</p>
    pub fn arguments(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.arguments.unwrap_or_default();
        v.push(input.into());
        self.arguments = ::std::option::Option::Some(v);
        self
    }
    /// <p>The command-line arguments to pass to the script. You can specify up to 20 arguments, and each argument can be up to 256 characters long.</p>
    pub fn set_arguments(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.arguments = input;
        self
    }
    /// <p>The command-line arguments to pass to the script. You can specify up to 20 arguments, and each argument can be up to 256 characters long.</p>
    pub fn get_arguments(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.arguments
    }
    /// <p>The behavior when the script fails. The default value is <code>TERMINATE</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>TERMINATE</code> – Terminates the compute node.</p></li>
    /// <li>
    /// <p><code>STOP_SEQUENCE</code> – Stops running subsequent scripts in the sequence but doesn't terminate the compute node.</p></li>
    /// <li>
    /// <p><code>CONTINUE</code> – Ignores the error and continues running the next script.</p></li>
    /// </ul>
    pub fn on_error(mut self, input: crate::types::OnError) -> Self {
        self.on_error = ::std::option::Option::Some(input);
        self
    }
    /// <p>The behavior when the script fails. The default value is <code>TERMINATE</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>TERMINATE</code> – Terminates the compute node.</p></li>
    /// <li>
    /// <p><code>STOP_SEQUENCE</code> – Stops running subsequent scripts in the sequence but doesn't terminate the compute node.</p></li>
    /// <li>
    /// <p><code>CONTINUE</code> – Ignores the error and continues running the next script.</p></li>
    /// </ul>
    pub fn set_on_error(mut self, input: ::std::option::Option<crate::types::OnError>) -> Self {
        self.on_error = input;
        self
    }
    /// <p>The behavior when the script fails. The default value is <code>TERMINATE</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>TERMINATE</code> – Terminates the compute node.</p></li>
    /// <li>
    /// <p><code>STOP_SEQUENCE</code> – Stops running subsequent scripts in the sequence but doesn't terminate the compute node.</p></li>
    /// <li>
    /// <p><code>CONTINUE</code> – Ignores the error and continues running the next script.</p></li>
    /// </ul>
    pub fn get_on_error(&self) -> &::std::option::Option<crate::types::OnError> {
        &self.on_error
    }
    /// <p>The policy that determines when the script runs. The default value is <code>FIRST_BOOT_ONLY</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FIRST_BOOT_ONLY</code> – Runs the script only the first time the compute node boots.</p></li>
    /// <li>
    /// <p><code>EVERY_BOOT</code> – Runs the script every time the compute node boots, including reboots.</p></li>
    /// </ul>
    pub fn execution_policy(mut self, input: crate::types::ExecutionPolicy) -> Self {
        self.execution_policy = ::std::option::Option::Some(input);
        self
    }
    /// <p>The policy that determines when the script runs. The default value is <code>FIRST_BOOT_ONLY</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FIRST_BOOT_ONLY</code> – Runs the script only the first time the compute node boots.</p></li>
    /// <li>
    /// <p><code>EVERY_BOOT</code> – Runs the script every time the compute node boots, including reboots.</p></li>
    /// </ul>
    pub fn set_execution_policy(mut self, input: ::std::option::Option<crate::types::ExecutionPolicy>) -> Self {
        self.execution_policy = input;
        self
    }
    /// <p>The policy that determines when the script runs. The default value is <code>FIRST_BOOT_ONLY</code>. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FIRST_BOOT_ONLY</code> – Runs the script only the first time the compute node boots.</p></li>
    /// <li>
    /// <p><code>EVERY_BOOT</code> – Runs the script every time the compute node boots, including reboots.</p></li>
    /// </ul>
    pub fn get_execution_policy(&self) -> &::std::option::Option<crate::types::ExecutionPolicy> {
        &self.execution_policy
    }
    /// Consumes the builder and constructs a [`NodeLifecycleScript`](crate::types::NodeLifecycleScript).
    /// This method will fail if any of the following fields are not set:
    /// - [`name`](crate::types::builders::NodeLifecycleScriptBuilder::name)
    pub fn build(self) -> ::std::result::Result<crate::types::NodeLifecycleScript, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::NodeLifecycleScript {
            name: self.name.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "name",
                    "name was not specified but it is required when building NodeLifecycleScript",
                )
            })?,
            script_source: self.script_source,
            arguments: self.arguments,
            on_error: self
                .on_error
                .unwrap_or("TERMINATE".parse::<crate::types::OnError>().expect("static value validated to member")),
            execution_policy: self.execution_policy.unwrap_or(
                "FIRST_BOOT_ONLY"
                    .parse::<crate::types::ExecutionPolicy>()
                    .expect("static value validated to member"),
            ),
        })
    }
}
