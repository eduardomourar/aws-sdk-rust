// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// When writing a match expression against `DomainEngineMode`, it is important to ensure
/// your code is forward-compatible. That is, if a match arm handles a case for a
/// feature that is supported by the service but has not been represented as an enum
/// variant in a current version of SDK, your code should continue to work when you
/// upgrade SDK to a future version in which the enum does include a variant for that
/// feature.
///
/// Here is an example of how you can make a match expression forward-compatible:
///
/// ```text
/// # let domainenginemode = unimplemented!();
/// match domainenginemode {
///     DomainEngineMode::General => { /* ... */ },
///     DomainEngineMode::Optimized => { /* ... */ },
///     other @ _ if other.as_str() == "NewFeature" => { /* handles a case for `NewFeature` */ },
///     _ => { /* ... */ },
/// }
/// ```
/// The above code demonstrates that when `domainenginemode` represents
/// `NewFeature`, the execution path will lead to the second last match arm,
/// even though the enum does not contain a variant `DomainEngineMode::NewFeature`
/// in the current version of SDK. The reason is that the variable `other`,
/// created by the `@` operator, is bound to
/// `DomainEngineMode::Unknown(UnknownVariantValue("NewFeature".to_owned()))`
/// and calling `as_str` on it yields `"NewFeature"`.
/// This match expression is forward-compatible when executed with a newer
/// version of SDK where the variant `DomainEngineMode::NewFeature` is defined.
/// Specifically, when `domainenginemode` represents `NewFeature`,
/// the execution path will hit the second last match arm as before by virtue of
/// calling `as_str` on `DomainEngineMode::NewFeature` also yielding `"NewFeature"`.
///
/// Explicitly matching on the `Unknown` variant should
/// be avoided for two reasons:
/// - The inner data `UnknownVariantValue` is opaque, and no further information can be extracted.
/// - It might inadvertently shadow other intended match arms.
///
/// <p>The engine mode for the domain. Valid values are <code>GENERAL</code> (the standard
/// Elasticsearch/OpenSearch engine) and <code>OPTIMIZED</code> (the cost- and
/// performance-optimized engine for observability and log-analytics workloads). If you don't
/// specify an engine mode, <code>GENERAL</code> is used. <code>OPTIMIZED</code> requires
/// OpenSearch 3.5 or later, OpenSearch Optimized instance types (OR1, OR2, OM2, or OI2) for the
/// data tier, and encryption at rest, and is available only for the <code>OBSERVABILITY</code>
/// and <code>MIXED</code> use cases. The engine mode can't be changed after the domain is
/// created.</p>
#[non_exhaustive]
#[derive(
    ::std::clone::Clone, ::std::cmp::Eq, ::std::cmp::Ord, ::std::cmp::PartialEq, ::std::cmp::PartialOrd, ::std::fmt::Debug, ::std::hash::Hash,
)]
pub enum DomainEngineMode {
    #[allow(missing_docs)] // documentation missing in model
    General,
    #[allow(missing_docs)] // documentation missing in model
    Optimized,
    /// `Unknown` contains new variants that have been added since this code was generated.
    #[deprecated(note = "Don't directly match on `Unknown`. See the docs on this enum for the correct way to handle unknown variants.")]
    Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue),
}
impl ::std::convert::From<&str> for DomainEngineMode {
    fn from(s: &str) -> Self {
        match s {
            "GENERAL" => DomainEngineMode::General,
            "OPTIMIZED" => DomainEngineMode::Optimized,
            other => DomainEngineMode::Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue(other.to_owned())),
        }
    }
}
impl ::std::str::FromStr for DomainEngineMode {
    type Err = ::std::convert::Infallible;

    fn from_str(s: &str) -> ::std::result::Result<Self, <Self as ::std::str::FromStr>::Err> {
        ::std::result::Result::Ok(DomainEngineMode::from(s))
    }
}
impl DomainEngineMode {
    /// Returns the `&str` value of the enum member.
    pub fn as_str(&self) -> &str {
        match self {
            DomainEngineMode::General => "GENERAL",
            DomainEngineMode::Optimized => "OPTIMIZED",
            DomainEngineMode::Unknown(value) => value.as_str(),
        }
    }
    /// Returns all the `&str` representations of the enum members.
    pub const fn values() -> &'static [&'static str] {
        &["GENERAL", "OPTIMIZED"]
    }
}
impl ::std::convert::AsRef<str> for DomainEngineMode {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}
impl DomainEngineMode {
    /// Parses the enum value while disallowing unknown variants.
    ///
    /// Unknown variants will result in an error.
    pub fn try_parse(value: &str) -> ::std::result::Result<Self, crate::error::UnknownVariantError> {
        match Self::from(value) {
            #[allow(deprecated)]
            Self::Unknown(_) => ::std::result::Result::Err(crate::error::UnknownVariantError::new(value)),
            known => Ok(known),
        }
    }
}
impl ::std::fmt::Display for DomainEngineMode {
    fn fmt(&self, f: &mut ::std::fmt::Formatter) -> ::std::fmt::Result {
        match self {
            DomainEngineMode::General => write!(f, "GENERAL"),
            DomainEngineMode::Optimized => write!(f, "OPTIMIZED"),
            DomainEngineMode::Unknown(value) => write!(f, "{value}"),
        }
    }
}
